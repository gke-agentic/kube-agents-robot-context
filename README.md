# kube-agents-robot-context

A context repository registered under `context_repos` on the kube-agents-robot live-test install
(`robot-host`). Read by the `obtainability-audit` stream for declared intent, never written to.
Declarations live under `docs/standards/`, named by `.kube-agents/intent.yaml`; the non-default
path is the point.
